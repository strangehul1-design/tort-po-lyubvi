; Лимиты загрузки для формы с фото-референсами.
; .user.ini, а не php_value в .htaccess: на хостингах с PHP-FPM
; php_value в .htaccess роняет сайт с ошибкой 500, а этот файл
; просто игнорируется там, где не поддерживается.
upload_max_filesize = 10M
post_max_size = 64M
max_file_uploads = 10
