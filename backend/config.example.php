<?php
/* ═══════════════════════════════════════════
   ОБРАЗЕЦ настроек обработчика заявок.

   Как пользоваться:
   1. В файловом менеджере хостинга создайте файл tort-config.php
      НА УРОВЕНЬ ВЫШЕ папки сайта — рядом с ней, а не внутри.
      Так его нельзя открыть по ссылке из браузера.
   2. Скопируйте туда это содержимое и впишите значения.

   Настоящий файл с ключами в репозиторий не попадает никогда.
   ═══════════════════════════════════════════ */

return [
    // Токен бота от @BotFather
    'TELEGRAM_BOT_TOKEN' => '',

    // Куда слать заявки. Несколько получателей — через запятую
    'TELEGRAM_CHAT_ID'   => '',

    // Пароль для просмотра заявок: /backend/requests.php?key=...
    // Придумайте длинный, не короче 20 символов
    'ADMIN_KEY'          => '',

    // ВКонтакте — по желанию, можно оставить пустым
    // 'VK_TOKEN'   => '',
    // 'VK_PEER_ID' => '',

    // Где хранить заявки. По умолчанию — папка tort-requests
    // рядом с этим файлом, вне папки сайта
    // 'STORAGE_DIR' => '/полный/путь/к/папке',

    // С каких адресов принимать заявки
    // 'ALLOWED_ORIGINS' => ['https://asiyatort.ru', 'https://www.asiyatort.ru'],
];
